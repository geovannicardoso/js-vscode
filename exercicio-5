const prompt = require ('prompt-sync')();

let soma=0
let num

do{
   num = Number (prompt('Digite o número: '));
    soma = soma +  num
} 
    while(num != 0 ){
   }  

console.log(`\n soma dos números digitados é: ${soma}`);
   